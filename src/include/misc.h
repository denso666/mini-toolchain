#ifndef LIB_MISC
#define LIB_MISC

#include <stdio.h>
#include <stdlib.h>

/**
 * @brief Verify a numeric interval value
 * @param nptr number pointer
 * @return On success, zero is returned. On error, one is returned.
 */
int valid_time_interval(const char*);


#endif
